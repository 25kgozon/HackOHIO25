# Build Vite frontend
FROM node:20-alpine AS build
WORKDIR /app
COPY src/frontend/vite-ui/package*.json ./
RUN npm ci
COPY src/frontend/vite-ui/ .
RUN npm run build

# Nginx runtime
FROM nginx:alpine
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
